import { RevupDirective } from './revup.directive';

describe('RevupDirective', () => {
  it('should create an instance', () => {
    const directive = new RevupDirective();
    expect(directive).toBeTruthy();
  });
});
